import { defineMessages } from "@trebired/i18n";

export default defineMessages({
    note:
    "Send a rough description, the deadline if there is one, and any " +
      "constraints you already know about.",
    prompt: "Tell me what you are building.",
});
